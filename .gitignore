bench/build/
*.bin
*.pt
sim
*.vvp
*.vcd
__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# Quartus
db/
incremental_db/
output_files/
simulation/
*.qws
*.rpt
*.sof
*.rbf
*.jdi
*.pin
*.summary
*.done

# checkpoints are re-downloadable; the generated headers are NOT ignored --
# Justin needs to `git pull` them and compile, without running python.
models/

# generated / vendored
vendor/run.c
dist/*.tar.gz

# never commit the ingest secret
*ingest-token*
.dev.vars

dist/*.zip
